# Mission Control keybindings, for the classic hyprland.conf format.

bind = CTRL, up,   exec, omarchy-shell shell toggle henri.missioncontrol '{}'
bind = CTRL, down, exec, omarchy-shell shell hide henri.missioncontrol
