# Mission Control keybindings for the legacy (hyprlang) Hyprland config.
# Any key works; F8 is only what was free here.
bind = , F8, event, mission-control toggle
bind = SHIFT, F8, event, mission-control toggle app
bind = CTRL, F8, event, mission-control toggle desktop
